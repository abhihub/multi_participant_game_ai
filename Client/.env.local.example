# Public — exposed to the browser
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_LIVEKIT_URL=wss://your-project.livekit.cloud

# Server-side only (never prefix with NEXT_PUBLIC_)
GAME_API_KEY=dev-api-key
